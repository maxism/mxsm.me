/** Shared OG / brand tokens (matches globals.css). */
export const OG = {
  bg: "#0a0907",
  ink: "#e8e2d2",
  inkMuted: "#9a9488",
  /** Void / signal accent — not time-of-day yellow. */
  accent: "#a88bf0",
  /** Static glitch RGB split (matches globals.css [data-glitch]). */
  glitchR: "#ff2a45",
  glitchB: "#00d9ff",
  rule: "rgba(232, 226, 210, 0.14)",
} as const;

export const OG_SIZE = { width: 1200, height: 630 } as const;
